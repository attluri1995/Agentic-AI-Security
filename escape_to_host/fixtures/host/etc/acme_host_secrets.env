# Simulated host secrets — NOT real credentials.
# In production this would live outside the container rootfs.
HOST_API_KEY=host_live_sk_acme_ESCAPE_9f2c
HOST_HOSTNAME=acme-build-host-01
DOCKER_SOCKET_EXPOSED=true
